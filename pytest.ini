[pytest]
markers =
    integration: tests that require downloading a real model (~500MB+) or hitting the network. Skipped by default. Run with `pytest -m integration` to include them, or set BIGSMALL_RUN_INTEGRATION=1.

# Skip integration tests unless explicitly requested.
addopts = -m "not integration"
